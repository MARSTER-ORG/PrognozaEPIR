name: Cleanup old Actions runs

on:
  workflow_dispatch:
  push:
    paths:
      - ".github/workflows/cleanup-actions.yml"
  schedule:
    - cron: "17 */12 * * *"

permissions:
  actions: write
  contents: read

concurrency:
  group: cleanup-old-actions-runs
  cancel-in-progress: false

jobs:
  cleanup:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Delete completed workflow runs older than 2 days
        uses: actions/github-script@v7
        with:
          script: |
            const owner = context.repo.owner;
            const repo = context.repo.repo;
            const cutoff = new Date(Date.now() - 2 * 24 * 60 * 60 * 1000);

            // Delete in bounded batches so the cleanup does not exhaust the
            // repository's GitHub REST API budget in a single run.
            const maxDeletes = 850;

            const runs = await github.paginate(
              github.rest.actions.listWorkflowRunsForRepo,
              {
                owner,
                repo,
                status: 'completed',
                per_page: 100,
              }
            );

            const oldRuns = runs
              .filter(run => new Date(run.created_at) < cutoff)
              .filter(run => Number(run.id) !== Number(context.runId))
              .sort((a, b) => new Date(a.created_at) - new Date(b.created_at));

            let deleted = 0;
            let failed = 0;
            let stoppedForRateLimit = false;

            for (const run of oldRuns) {
              if (deleted >= maxDeletes) break;

              try {
                await github.rest.actions.deleteWorkflowRun({
                  owner,
                  repo,
                  run_id: run.id,
                });
                deleted += 1;
              } catch (error) {
                const remaining = Number(error?.response?.headers?.['x-ratelimit-remaining'] ?? -1);
                if (error.status === 403 && remaining >= 0 && remaining < 20) {
                  stoppedForRateLimit = true;
                  core.warning('Stopping cleanup before hitting the GitHub API rate limit.');
                  break;
                }

                failed += 1;
                core.warning(`Could not delete run ${run.id}: ${error.message}`);
              }
            }

            const remainingOld = Math.max(0, oldRuns.length - deleted);

            await core.summary
              .addHeading('GitHub Actions cleanup')
              .addTable([
                [{data: 'Cutoff', header: true}, cutoff.toISOString()],
                [{data: 'Old completed runs found', header: true}, String(oldRuns.length)],
                [{data: 'Deleted in this run', header: true}, String(deleted)],
                [{data: 'Remaining old runs', header: true}, String(remainingOld)],
                [{data: 'Delete failures', header: true}, String(failed)],
                [{data: 'Stopped for rate limit', header: true}, String(stoppedForRateLimit)],
              ])
              .write();

            core.info(`Found ${oldRuns.length} completed runs older than ${cutoff.toISOString()}.`);
            core.info(`Deleted ${deleted}; remaining old runs: ${remainingOld}; failures: ${failed}.`);
