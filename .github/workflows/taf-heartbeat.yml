name: EPIR neighbor TAF archive watchdog

on:
  schedule:
    - cron: '47 5,11,17,23 * * *'
  workflow_dispatch:

permissions:
  contents: read
  actions: write

concurrency:
  group: prognozaepir-taf-watchdog
  cancel-in-progress: true

jobs:
  watchdog:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Check TAF freshness and refresh GitHub only from Railway
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          if python3 scripts/check_epir_archive_freshness.py --taf-only --all-tafs; then
            echo 'TAF archive is fresh.'
            exit 0
          fi
          echo '::warning::TAF archive stale; requesting a validated Railway-to-GitHub mirror refresh'
          gh workflow run mirror-central-archive.yml --repo "${{ github.repository }}" --ref main
          exit 1
