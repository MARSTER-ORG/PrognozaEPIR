name: Legacy meteo.mil import redirect
on:
  workflow_dispatch:
permissions:
  contents: read
  actions: write
jobs:
  redirect:
    runs-on: ubuntu-latest
    steps:
      - env:
          GH_TOKEN: ${{ github.token }}
        run: gh workflow run message-archive.yml --repo "${{ github.repository }}" --ref main
