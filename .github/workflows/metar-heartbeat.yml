name: EPIR METAR SPECI archive watchdog

on:
  schedule:
    - cron: '7-57/5 * * * *'
  workflow_dispatch:

permissions:
  contents: read
  actions: write

concurrency:
  group: prognozaepir-metar-watchdog
  cancel-in-progress: true

jobs:
  watchdog:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Check freshness and trigger the single ingestor if needed
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          if python3 scripts/check_epir_archive_freshness.py --metar-only; then
            echo 'METAR/SPECI archive is fresh.'
            exit 0
          fi
          echo '::warning::METAR/SPECI archive stale; triggering central ingestor'
          gh workflow run message-archive.yml --repo "${{ github.repository }}" --ref main
          exit 1
