name: One-shot single PROB30 and fog event fix

on:
  push:
    branches:
      - fix/single-prob30-fog-event-learning-2026-09-14

permissions:
  contents: write

jobs:
  apply:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "temporarily disabled while migration is corrected"
