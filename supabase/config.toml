project_id = "PrognozaEPIR"

# Railway authenticates this service-to-service endpoint with x-ingest-token.
# The function performs that check before creating a service-role client.
[functions.message-ingest]
verify_jwt = false
